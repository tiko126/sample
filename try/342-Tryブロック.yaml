# TRY
+try_1:
  try>: error1
  _do:

    # テキストを作成
    +text_2:
      action>: Text
      text: ${hoge}

    # TRY
    +try_2:
      try>: erro2
      _do:

        # テキストを作成
        +text_3:
          action>: Text
          text: error2

        # TRY
        +try_3:
          try>: erro3
          _do:

            # テキストを作成
            +text_1:
              action>: Text
              text: error3